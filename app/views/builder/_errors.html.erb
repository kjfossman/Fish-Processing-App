<% if vessel.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(vessel.errors.count, "error") %> prohibited this boat from being saved:</h2>

  <ul>
  <% vessel.errors.full_messages.each do |msg| %>
    <li><%= msg %></li>
  <% end %>
  </ul>
</div>
<% end %>